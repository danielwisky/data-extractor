<?xml version='1.0' encoding='utf-8'?>

<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- Database connection settings -->
        <property name="connection.driver_class">org.h2.Driver</property>
        <property name="connection.url">jdbc:h2:./data/db</property>
        <property name="connection.username">sa</property>
        <property name="connection.password"/>

        <!-- JDBC connection pool (use the built-in) -->
        <property name="connection.pool_size">1</property>

        <!-- SQL dialect -->
        <property name="dialect">org.hibernate.dialect.H2Dialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="show_sql">true</property>

        <!-- Drop and re-create the database schema on startup -->
        <property name="hbm2ddl.auto">update</property>

        <mapping class="br.com.danielwisky.extractor.domains.bible.Bible"/>
        <mapping class="br.com.danielwisky.extractor.domains.bible.Book"/>
        <mapping class="br.com.danielwisky.extractor.domains.bible.Chapter"/>
        <mapping class="br.com.danielwisky.extractor.domains.bible.Verse"/>
        <mapping class="br.com.danielwisky.extractor.domains.vehicle.Brand"/>
        <mapping class="br.com.danielwisky.extractor.domains.vehicle.Metric"/>
        <mapping class="br.com.danielwisky.extractor.domains.vehicle.Model"/>
        <mapping class="br.com.danielwisky.extractor.domains.vehicle.Vehicle"/>
    </session-factory>

</hibernate-configuration>